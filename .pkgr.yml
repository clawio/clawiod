targets:
  ubuntu-14.04:
    after:
      - cp config.core.json /etc/clawiod/config.core.json
      - cp config.fileauth.json /etc/clawiod/clawio.d/config.fileauth.json
      - mkdir -p /home/clawiod/data/notmp
      - mkdir -p /home/clawiod/data/tmp
  ubuntu-12.04:
  centos-6:
  centos-7:
  debian-7:
  debian-8:
  fedora-20:
  sles-12:

user: clawiod
group: clawiod

cli: clawiod


