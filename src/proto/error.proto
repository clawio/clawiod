syntax = "proto3";

package proto;

enum ErrorCode {
	OK = 0;
	INTERNAL = 1;
	CREDENTIALS_INVALID = 10;
	FILE_NOT_FOUND = 20;
	FILE_EXISTS = 21;
	USER_NOT_FOUND = 30;
    TOKEN_INVALID = 40;
}

message Error {
	ErrorCode code = 1;
	string message = 2;
	string user = 3;
}
